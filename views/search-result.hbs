<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Shipwrecks Data</title>
  <style>
    /* Add some basic CSS styles */
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      margin: 0;
    }
    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      overflow-x: auto;
    }
    table {
      border-collapse: collapse;
      width: 100%;
      margin-top: 20px;
      max-width: 100%;
    }
    th, td {
      text-align: left;
      padding: 8px;
      word-wrap: break-word;
    }
    th {
      background-color: #f2f2f2;
    }
    tr:nth-child(even) {
      background-color: #f2f2f2;
    }
    tr:hover {
      background-color: #ddd;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Shipwrecks Data</h1>
    <form method="POST" action="/api/search">
      <label for="depth">Filter by Depth:</label>
      <input type="number" id="depth" name="depth">
      <button type="submit">Filter</button>
    </form>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Record</th>
          <th>Vessel Terms</th>
          <th>Feature Type</th>
          <th>Chart</th>
          <th>Latitude</th>
          <th>Longitude</th>
          <th>Depth</th>
          <th>Sounding Type</th>
        </tr>
      </thead>
      <tbody>
        {{#each shipwrecks}}
          <tr>
            <td>{{this._id}}</td>
            <td>{{this.recrd}}</td>
            <td>{{this.vesslterms}}</td>
            <td>{{this.feature_type}}</td>
            <td>{{this.chart}}</td>
            <td>{{this.latdec}}</td>
            <td>{{this.londec}}</td>
            <td>{{this.depth}}</td>
            <td>{{this.sounding_type}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
    {{#unless shipwrecks}}
      <p>No shipwrecks found.</p>
    {{/unless}}
  </div>
</body>
</html>
